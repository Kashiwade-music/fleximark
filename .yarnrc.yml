nodeLinker: node-modules

# Preserve npm's install-script behavior during the initial migration. This is
# required by tools such as esbuild and can be narrowed to an allowlist later.
enableScripts: true
